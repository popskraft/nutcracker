# AI_AGENT_SYSTEM_BOOTSTRAP.md

schema_version: 1
Status: legacy compatibility document.

## Authoritative Bootstrap Contract
Use `AGENTS_INIT.md` as the single source of truth for AI workspace bootstrap.

## Why This File Exists
This file is kept for backward compatibility with older references.
It must not diverge from `AGENTS_INIT.md` in process decisions.

## Redirect Rule
If this file is opened first:
1. Read `AGENTS_INIT.md`.
2. Apply bootstrap policy from `AGENTS_INIT.md`.
3. Use `AGENTS.md` as runtime contract after bootstrap.
